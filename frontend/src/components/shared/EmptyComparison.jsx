export default function EmptyComparison({ children }) {
  return (
    <section className="rounded-xl border border-dashed border-slate-300 bg-white p-8 text-center text-sm font-semibold text-slate-500">
      {children}
    </section>
  )
}
